import { extendTheme } from '@chakra-ui/react';

const customTheme = extendTheme({
  colors: {
    color: {
      blue: '#3770FB',
      yellow: '#FFE500',
      red: '#FF665B',
      gray_1: '#B7B7B7',
      gray_2: '#505050',
    },
  },
});

export default customTheme;
